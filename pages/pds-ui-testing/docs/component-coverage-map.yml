routes:
  "/":
    purpose: "Minimal home baseline route."
    components:
      - PHeading
    scenarios:
      - "Route renders heading"
      - "Route links to overview and checkout"
  "/overview":
    purpose: "Minimal overview baseline route."
    components:
      - PHeading
    scenarios:
      - "Route renders heading"
      - "Route links to detail"
  "/detail/[id]":
    purpose: "Minimal detail baseline route."
    components:
      - PHeading
    scenarios:
      - "Route renders heading including id"
      - "Route links to checkout"
  "/checkout":
    purpose: "Minimal checkout baseline route."
    components:
      - PHeading
    scenarios:
      - "Route renders heading"
      - "Route links to home"

testing:
  automated:
    - "Playwright + Axe route smoke tests"
    - "Aria snapshot for home page landmark structure"
  manual:
    - "Screen reader announcement checks for all routes"
    - "Keyboard navigation between route links"
